# CT Turnout Modelling

Builds a CT-level modelling master dataset from the existing census, interpolation, and election outputs.

Run from `analysis/toronto_election_turnout/`:

```bash
npm run build:modelling
```
